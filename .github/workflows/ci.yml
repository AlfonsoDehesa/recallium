name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: uv sync --group dev

      - name: Check formatting
        run: uv run ruff format --check .

      - name: Lint
        run: uv run ruff check .

      - name: Type check
        run: uv run pyright

      - name: Test
        run: uv run pytest --cov=src/recollectium --cov-report=term-missing

  install-smoke:
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-x86_64
            os: ubuntu-latest
            shell: bash
          - name: linux-arm64
            os: ubuntu-24.04-arm
            shell: bash
          - name: macos-intel
            os: macos-14
            shell: bash
          - name: macos-apple-silicon
            os: macos-15
            shell: bash
          - name: windows-x86_64
            os: windows-latest
            shell: pwsh
          - name: windows-arm64
            os: windows-11-arm
            shell: pwsh
    name: install-smoke (${{ matrix.name }})
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Recollectium on Unix
        if: runner.os != 'Windows'
        shell: bash
        run: sh ./install.sh
        env:
          RECOLLECTIUM_INSTALL_REF: ${{ github.head_ref || github.ref_name }}

      - name: Install Recollectium on Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: .\install.ps1
        env:
          RECOLLECTIUM_INSTALL_REF: ${{ github.head_ref || github.ref_name }}

      - name: Verify Recollectium on Unix
        if: runner.os != 'Windows'
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          recollectium --version
          recollectium upgrade --check --install-method bootstrap --repo "$GITHUB_REPOSITORY" > upgrade-check.json
          recollectium upgrade --dry-run --install-method bootstrap --repo "$GITHUB_REPOSITORY" > upgrade-dry-run.json
          python - <<'PY'
          import json
          from pathlib import Path
          for path in ["upgrade-check.json", "upgrade-dry-run.json"]:
              payload = json.loads(Path(path).read_text())
              assert payload["status"] in {"up_to_date", "dry_run"}, payload
              assert payload["install_method"] == "bootstrap", payload
          PY

      - name: Verify uninstall on Unix
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          export PATH="$HOME/.local/bin:$PATH"

          python - <<'PY'
          import json
          from pathlib import Path

          metadata_path = Path.home() / ".local" / "state" / "recollectium" / "install.json"
          metadata = json.loads(metadata_path.read_text(encoding="utf-8"))
          completion_targets = []
          for item in metadata.get("managed_completion_edits", []):
              if not isinstance(item, dict):
                  continue
              path = item.get("path")
              if isinstance(path, str) and path:
                  completion_targets.append(path)
          if not completion_targets:
              for item in metadata.get("managed_path_edits", []):
                  if not isinstance(item, str) or "completion --source" not in item:
                      continue
                  path, _, _command = item.partition(": ")
                  if path:
                      completion_targets.append(path)
          assert completion_targets, metadata
          for path_text in completion_targets:
              path = Path(path_text)
              content = path.read_text(encoding="utf-8")
              assert "# >>> recollectium completion >>>" in content, content
          Path("completion-targets.json").write_text(json.dumps(completion_targets), encoding="utf-8")
          PY

          recollectium add --space user --type note --content "CI uninstall smoke sentinel" > add-default.json
          recollectium uninstall > uninstall-default.json 2> uninstall-default.stderr

          python - <<'PY'
          import json
          from pathlib import Path

          add_payload = json.loads(Path("add-default.json").read_text(encoding="utf-8"))
          assert add_payload.get("id"), add_payload

          payload = json.loads(Path("uninstall-default.json").read_text(encoding="utf-8"))
          assert Path("uninstall-default.stderr").read_text(encoding="utf-8") == ""
          assert payload["status"] == "manual_uninstall_required", payload
          assert payload["package"]["install_method"] == "bootstrap", payload
          assert payload["package"]["recommended"] == "uv tool uninstall recollectium", payload
          assert payload["data"]["preserved"] is True, payload
          paths = payload["data"]["paths"]
          assert Path(paths["config"]).exists(), paths
          assert Path(paths["database"]).exists(), paths

          completion_targets = json.loads(Path("completion-targets.json").read_text(encoding="utf-8"))
          for path_text in completion_targets:
              content = Path(path_text).read_text(encoding="utf-8")
              assert "# >>> recollectium completion >>>" not in content, content
          removed = payload["shell_completion"]["removed"]
          assert any(item.get("removed") is True and item.get("blocks", 0) >= 1 for item in removed), payload
          PY

          # The default bootstrap uninstall now starts a package-removal handoff, so
          # wait for that delayed handoff, clear any partially removed tool env,
          # and reinstall the checked-out package before running the purge half of
          # this smoke test.
          sleep 2
          uv tool uninstall recollectium || true
          uv tool install --force .
          recollectium --version

          recollectium init
          recollectium add --space user --type note --content "CI purge uninstall smoke sentinel" > add-purge.json
          recollectium uninstall --purge --yes-delete-all-recollectium-data > uninstall-purge.json 2> uninstall-purge.stderr

          python - <<'PY'
          import json
          from pathlib import Path

          add_payload = json.loads(Path("add-purge.json").read_text(encoding="utf-8"))
          assert add_payload.get("id"), add_payload

          payload = json.loads(Path("uninstall-purge.json").read_text(encoding="utf-8"))
          purge_stderr = Path("uninstall-purge.stderr").read_text(encoding="utf-8")
          assert "The following Recollectium-owned paths will be permanently deleted:" in purge_stderr, purge_stderr
          assert payload["status"] == "manual_uninstall_required", payload
          assert payload["package"]["install_method"] == "bootstrap", payload
          assert payload["package"]["recommended"] == "uv tool uninstall recollectium", payload
          assert payload["data"]["preserved"] is False, payload
          purge = payload["data"]["purge"]
          assert purge["dry_run"] is False, payload
          assert purge["deleted"], payload
          paths = payload["data"]["paths"]
          assert not Path(paths["config"]).exists(), paths
          assert not Path(paths["database"]).exists(), paths
          PY

          uv tool uninstall recollectium
          hash -r
          if command -v recollectium >/dev/null 2>&1; then
            echo "recollectium command still exists after uv tool uninstall" >&2
            command -v recollectium >&2 || true
            exit 1
          fi

      - name: Verify Recollectium on Windows
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          recollectium --version
          recollectium upgrade --check --install-method bootstrap --repo "$env:GITHUB_REPOSITORY" | Out-File -Encoding utf8 upgrade-check.json
          recollectium upgrade --dry-run --install-method bootstrap --repo "$env:GITHUB_REPOSITORY" | Out-File -Encoding utf8 upgrade-dry-run.json
          python -c "import json, pathlib; payloads=[json.loads(pathlib.Path(p).read_text(encoding='utf-8-sig')) for p in ['upgrade-check.json','upgrade-dry-run.json']]; assert all(p['status'] in {'up_to_date', 'dry_run'} for p in payloads), payloads; assert all(p['install_method'] == 'bootstrap' for p in payloads), payloads"

      - name: Verify uninstall on Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          function Invoke-RecollectiumSmokeCommand {
            param(
              [string[]]$Arguments,
              [string]$StdoutPath,
              [string]$StderrPath
            )
            & recollectium @Arguments > $StdoutPath 2> $StderrPath
            if ($LASTEXITCODE -ne 0) {
              if (Test-Path $StdoutPath) { Get-Content $StdoutPath }
              if (Test-Path $StderrPath) { Get-Content $StderrPath }
              throw "recollectium $($Arguments -join ' ') failed with exit code $LASTEXITCODE"
            }
          }

          function Invoke-PythonSmokeCheck {
            param([string]$Command)
            python -c $Command
            if ($LASTEXITCODE -ne 0) {
              throw "python smoke assertion failed with exit code $LASTEXITCODE"
            }
          }

          Invoke-RecollectiumSmokeCommand -Arguments @("add", "--space", "user", "--type", "note", "--content", "CI uninstall smoke sentinel") -StdoutPath add-default.json -StderrPath add-default.stderr
          Invoke-RecollectiumSmokeCommand -Arguments @("uninstall") -StdoutPath uninstall-default.json -StderrPath uninstall-default.stderr

          Invoke-PythonSmokeCheck -Command "import json, pathlib; add=json.loads(pathlib.Path('add-default.json').read_text(encoding='utf-8-sig')); assert add.get('id'), add; assert pathlib.Path('uninstall-default.stderr').read_text(encoding='utf-8-sig') == ''; payload=json.loads(pathlib.Path('uninstall-default.json').read_text(encoding='utf-8-sig')); assert payload['status'] == 'manual_uninstall_required', payload; assert payload['package']['install_method'] == 'bootstrap', payload; assert payload['package']['recommended'] == 'uv tool uninstall recollectium', payload; assert payload['data']['preserved'] is True, payload; paths=payload['data']['paths']; assert pathlib.Path(paths['config']).exists(), paths; assert pathlib.Path(paths['database']).exists(), paths"

          $uv = (Get-Command uv -ErrorAction SilentlyContinue).Source
          if (-not $uv) { $uv = Join-Path $env:LOCALAPPDATA "uv\uv.exe" }

          # The default bootstrap uninstall now starts a package-removal handoff, so
          # wait for that delayed handoff, clear any partially removed tool env,
          # and reinstall the checked-out package before running the purge half of
          # this smoke test.
          Start-Sleep -Seconds 2
          & $uv tool uninstall recollectium
          & $uv tool install --force .
          if ($LASTEXITCODE -ne 0) { throw "uv tool reinstall failed with exit code $LASTEXITCODE" }
          recollectium --version

          Invoke-RecollectiumSmokeCommand -Arguments @("init") -StdoutPath init-purge.json -StderrPath init-purge.stderr
          Invoke-PythonSmokeCheck -Command "import pathlib; assert pathlib.Path('init-purge.stderr').read_text(encoding='utf-8-sig') == ''"
          Invoke-RecollectiumSmokeCommand -Arguments @("add", "--space", "user", "--type", "note", "--content", "CI purge uninstall smoke sentinel") -StdoutPath add-purge.json -StderrPath add-purge.stderr
          Invoke-RecollectiumSmokeCommand -Arguments @("uninstall", "--purge", "--yes-delete-all-recollectium-data") -StdoutPath uninstall-purge.json -StderrPath uninstall-purge.stderr

          Invoke-PythonSmokeCheck -Command "import json, pathlib; add=json.loads(pathlib.Path('add-purge.json').read_text(encoding='utf-8-sig')); assert add.get('id'), add; purge_stderr=pathlib.Path('uninstall-purge.stderr').read_text(encoding='utf-8-sig'); assert 'The following Recollectium-owned paths will be permanently deleted:' in purge_stderr, purge_stderr; payload=json.loads(pathlib.Path('uninstall-purge.json').read_text(encoding='utf-8-sig')); assert payload['status'] == 'manual_uninstall_required', payload; assert payload['package']['install_method'] == 'bootstrap', payload; assert payload['package']['recommended'] == 'uv tool uninstall recollectium', payload; assert payload['data']['preserved'] is False, payload; purge=payload['data']['purge']; assert purge['dry_run'] is False, payload; assert purge['deleted'], payload; paths=payload['data']['paths']; assert not pathlib.Path(paths['config']).exists(), paths; assert not pathlib.Path(paths['database']).exists(), paths"

          & $uv tool uninstall recollectium
          if ($LASTEXITCODE -ne 0) { throw "uv tool uninstall failed with exit code $LASTEXITCODE" }
          if (Get-Command recollectium -ErrorAction SilentlyContinue) {
            throw "recollectium command still exists after uv tool uninstall"
          }
